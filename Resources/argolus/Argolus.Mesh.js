Argolus.Mesh = new Class({
    markers : [],
    Implements: Options,
    options: {
        color: '#fff',
        size: {
            width: 100,
            depth: 100
        }
    },
    initialize : function(options){
        this.setOptions(options);
        
    },
    move : function(x, y){
        if(typeOf(marker) == 'number') marker = markers[marker];
        
    },
    z : function(x, y){
        
    }
});
